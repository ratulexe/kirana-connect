/** Shared labels so /register and /onboarding show one continuous journey. */
export const WIZARD_STEPS = ["Account", "Store", "Address", "Hours", "Review"];

/** /onboarding covers steps 2-5, so its local index is offset by the account step. */
export const ONBOARDING_STEP_OFFSET = 1;
